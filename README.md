# A little triangle rasterizer and 3d renderer
## Usage
- NOTE: This wasn't mean to be a library, so I wouldn't use it outside of the
project.
- NOTE: This requires `SDL2`.
- Just run `make test` for the demo.
